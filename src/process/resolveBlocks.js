/**
 * 
 * @param {object} obj 
 * @return {object}
 */
function resolveBlocks(obj){
  var mergeLinks = require("./resolve/mergeLinks");
  var resolveDates = require("./resolve/resolveDates");
  //merge links to each version's objects
   
  //resolve issue dates
   
  return obj;
}

module.exports = resolveBlocks;